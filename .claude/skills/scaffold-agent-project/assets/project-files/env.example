# Template — the scaffolder writes this as `.env.example`. Copy to `.env` and fill in.
# `.env` is gitignored; `.env.example` is committed so others know what's required.
# The secret-scan hook blocks reading/committing real secrets.

# {{FILL: list the env vars this project needs, with safe placeholder values}}
# Examples:
# DATABASE_URL="file:./local.db"
# API_KEY="your-key-here"
